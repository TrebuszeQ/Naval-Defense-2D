import { LevelDescriptionPipe } from './level-description.pipe';

describe('LevelDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new LevelDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
