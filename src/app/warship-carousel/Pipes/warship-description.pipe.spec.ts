import { WarshipDescriptionPipe } from './warship-description.pipe';

describe('WarshipDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new WarshipDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
